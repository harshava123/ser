name: Deploy Backend to Server

on:
  push:
    branches:
      - main
    paths:
      - 'routes/**'
      - 'models/**'
      - 'server.js'
      - 'package.json'
      - 'Dockerfile'
      - 'docker-compose.yml'
      - '.github/workflows/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Add server to known hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
        chmod 600 ~/.ssh/known_hosts

    - name: Deploy to server
      run: |
        ssh -o StrictHostKeyChecking=accept-new ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
          cd /data/backend
          git pull origin main
          docker-compose down
          docker-compose build --no-cache
          docker-compose up -d
          sleep 5
          docker-compose ps
          docker-compose logs --tail=50 backend
        EOF

    - name: Health check
      run: |
        echo "Waiting for backend to be ready..."
        sleep 15
        echo "Testing health endpoint on port 80..."
        if ! curl -f http://${{ secrets.SERVER_IP }}/api/health; then
          echo "Port 80 failed, trying direct port 5000..."
          curl -f http://${{ secrets.SERVER_IP }}:5000/api/health || exit 1
        fi

